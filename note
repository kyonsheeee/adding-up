fs: FileSystem の略。ファイルを扱うためのモジュール
readline: ファイルを一行ずつ読み込むためのモジュール

Stream とは
Node.js では入出力が発生する処理を Stream という形で扱う。
Stream とは、非同期で情報を取り扱うための概念で、情報自体ではなく情報の流れに注目

API
応用ソフトウェアから利用可能なインタフェース...?

for-of 構文
Map や Array の中身を of の前で与えられた変数に代入することで for ループと同じ処理をする書き方

sort 関数、比較関数
比較関数は 2 つの引数を受けとって、
前者の引数 pair1 を 後者の引数 pair2 より前にしたいときは、負の整数、
pair2 を pair1 より前にしたいときは、正の整数、
pair1 と pair2 の並びをそのままにしたいときは 0 を返す必要がある。

map 関数は、Array の要素それぞれを、与えられた関数を適用したないように変換する
配列.map(関数);